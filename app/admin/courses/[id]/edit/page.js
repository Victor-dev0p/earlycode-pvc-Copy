import EditCourseClient from './edit';

export const metadata = {
  title: 'Build Curriculum - Admin - PVC',
  description: 'Build course curriculum',
};

export default function EditCoursePage() {
  return <EditCourseClient />;
}