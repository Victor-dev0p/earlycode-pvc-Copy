import DashboardClient from './dashboard';

export const metadata = {
  title: 'Dashboard - PVC',
  description: 'Your PVC dashboard',
};

export default function DashboardPage() {
  return <DashboardClient />;
}